#include <iostream>

using namespace std;

int main()
{
  setlocale(LC_ALL, "Russian");
  cout << "Ââåäèòå À è Â: ";

  double a, b;

  cin >> a >> b;

  a *= a; b *= b;
  cout << a + b << ' ' << a - b << ' ' << a * b << ' ' << a / b;

  return 0;
}
