#include <iostream>

using namespace std;

int Sign(double x){
    if(x < 0) return -1;
    if(x > 0) return 1;
    return 0;
}

int main()
{

    double a, b;
    cout << "Enter A and B: ";
    cin >> a >> b;

    cout << "Result: " << Sign(a) + Sign(b);

    return 0;
}
