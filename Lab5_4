#include <iostream>

using namespace std;

int main()
{

    int a, b;
    do{
        cout << "Enter nnumber: ";
        cin >> a;
    }while(a / 10 == 0 || a / 10 >= 10);

    b = a % 10;
    a /= 10;

    cout << "Result: " << b * 10 + a;

    return 0;
}
