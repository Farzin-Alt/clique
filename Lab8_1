#include <iostream>

using namespace std;

int main()
{

    int a, b;
    cout << "Enter A and B: ";
    cin >> a >> b;

    if(a == b){ a = 0; b = 0; }
    else if(a > b) b = a;
    else a = b;

    cout << "New A: " << a << " New B: " << b;

    return 0;
}
